---
description: Public IPs table provides a list of all existing IPv4 and IPv6 public IP addresses in AWS, Azure and GCP
---

# Public IP Addresses

There are two tables that provide a list of all IPv4 and IPv6 public IP addresses created in public clouds.
They show the IP name, region, information on whether the public IP is in use, and, if so, the related resource type and ID.

![Technology table showing Cloud Public IP addresses](../../../images/technology/IP_Fabric_GUI-technology_tables-cloud_public-ips.webp)
