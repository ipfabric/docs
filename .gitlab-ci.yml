image: alpine:3.13
pages:
  stage: deploy
  only:
    - gh-pages
  script:
    - mkdir .public
    - cp -ra * .public/
    - mv .public public
  artifacts:
    expire_in: "5 days"
    paths:
      - public
